---
'create-rn-miniapp': patch
---

Add Supabase-aware frontend and backoffice bootstrap files during scaffold, migrate source patching to SWC AST transforms, and ensure Granite watches the monorepo root via `metro.watchFolders`.
