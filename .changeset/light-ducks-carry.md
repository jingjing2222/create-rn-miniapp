---
'create-rn-miniapp': patch
'@create-rn-miniapp/scaffold-templates': patch
---

Add `--add` mode so existing workspaces can attach missing `server` and `backoffice` apps after the initial scaffold.
