# Firebase Functions Overview

- source of truth: `server/.create-rn-miniapp/state.json`, `server/README.md`
- runtime surface: `server/firebase.json`, `server/firestore.rules`, `server/functions/src/`
- client linkage는 `client-connection.md`, 로컬 실행은 `local-dev.md`, 문제 해결은 `troubleshooting.md`로 본다.
