# Cloudflare Worker Overview

- source of truth: `server/.create-rn-miniapp/state.json`, `server/README.md`
- runtime entry: `server/src/index.ts`
- bindings: `server/wrangler.jsonc`
- local test config: `server/wrangler.vitest.jsonc`, `server/vitest.config.mts`
- client linkage는 `client-connection.md`, 로컬 실행은 `local-dev.md`, 이상 징후는 `troubleshooting.md`로 본다.
